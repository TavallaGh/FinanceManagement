---
title: "speckit.taskclose"
description: "Close and review-ready a completed task: transition Jira to 'In Review', mark GitLab MRs as ready, generate Postman collections if needed, and create task close logs. MUST run after taskCompletion (03) completes successfully."
tags:
  - task
  - close
  - jira-workflow
  - gitlab-workflow
inputs:
  - name: reference_id
    description: "Jira task or story reference id (e.g., AC-28 or AC-28-01)"
    required: true
  - name: generate_postman
    description: "Set to 'true' if task added new API endpoints requiring Postman collection generation (default: auto-detect)"
    required: false
---

### Reusable Script Policy (Mandatory)

- Use reusable scripts in `scripts/`.
- Do NOT create ad-hoc/new scripts for this command.
- Reuse `scripts/task-exec.ps1` or `scripts/task-exec.sh` for Jira/GitLab operational flow when applicable.
- Reuse `scripts/create-jira-subtask.ps1` for Jira task/subtask create-update operations when applicable.
- Credentials must be loaded from `.secrets/credentials.local`.

## Purpose

Single unified taskclose workflow that completes ALL task closure duties:
1. **Transition Jira task to "In Review" status** and add comment
2. **Mark GitLab MRs as Ready for review** (from Draft)
3. **Generate Postman collection** for API endpoints (story-level, in implementation folder)
4. **Generate `<TASK-KEY>-taskclose.md`** — the single comprehensive close record for the task

All duties execute in one command with no manual intervention required.

## Mandatory Precondition Check (VERIFICATION ONLY — NO INVOCATION)

**CRITICAL: TaskClose does NOT call or invoke taskCompletion. It ONLY verifies the file exists.**

**STOP IMMEDIATELY if ANY of the following is true:**
- TaskCompletion (03) completion file does NOT exist at `docs/work-items/03.completation/linked/stories/<PARENT-STORY>/Tasks/<TASK>/completion.md`
- Jira task is NOT in `In Progress` status
- Either workspace or project MR is missing from Jira Web Links

If file check fails:
1. Do NOT attempt to run or invoke taskCompletion
2. Stop and report error: "Precondition Failed: completion.md does not exist at [path]"
3. Instruct user: "Please run taskCompletion (03) first, then retry taskClose (04)"
4. Exit without making any changes to Jira or GitLab

## Required Behavior (EXECUTE IN THIS ORDER)

### PHASE 0: Precondition Verification (BEFORE ALL ELSE)

1. Check if file exists: `docs/work-items/03.completation/linked/stories/<PARENT-STORY>/Tasks/<TASK>/completion.md`
   - If NOT found: **STOP immediately** — report error and exit
   - If found: Continue to Phase 1

### PHASE 1: Jira Workflow Update (FIRST)

1. Parse `$ARGUMENTS` as Jira task key or Jira URL.
2. Load credentials from `.secrets/credentials.local`.
3. Fetch Jira task details:
   - Verify task is in `In Progress` status
   - Extract parent story ID (if task)
   - Get GitLab issue and MR links from Jira Web Links
   - Capture current timestamps
4. **Transition Jira task to "In Review"** status using task-exec.ps1:
   - Use correct Jira transition ID from credentials
   - Add Jira comment: "Task ready for review. See task close record at `docs/work-items/02.implementation/stories/<STORY>/tasks/<TASK>-taskclose.md`"

### PHASE 2: GitLab MR State Update (SECOND)

5. Fetch both MRs from Jira Web Links:
   - Workspace MR (documentation/process artifacts)
   - Project MR (product code changes)
6. **Update MR states from Draft to Ready:**
   - Set MR state to "Ready for Review" (if currently Draft)
   - Verify both MRs have `Delete source branch` and `Squash commits` enabled
   - Add MR note: "Transitioned to In Review by taskClose on [timestamp]"

### PHASE 3: Postman Collection Generation (THIRD)

7. **Automatically detect and document backend API endpoints:**
   - **Scan backend repositories** for new/modified endpoints:
     * C# Controllers: `[HttpGet]`, `[HttpPost]`, `[HttpPut]`, `[HttpDelete]`, `[HttpPatch]` decorators
     * Minimal APIs: `.MapGet()`, `.MapPost()`, `.MapPut()`, `.MapDelete()`, `.MapPatch()` patterns
     * Route attributes: `[Route]` and `[HttpRoute]`
     * Scan in: `Controllers/`, `Endpoints/`, `Features/` folders
   - **Generate or update story-level Postman collection** at:
     `docs/work-items/02.implementation/stories/<STORY>/postman/<STORY>.postman_collection.json`
   - **Postman collection must include:**
     * All detected new/modified endpoints (method, path)
     * Request body examples (if POST/PUT/PATCH)
     * Response schema documentation
     * Authentication headers (Bearer Token, API Key, etc.)
     * Environment variables:
       - `base_url`: Dev/Test/Prod URLs
       - `auth_token`: Authorization token
       - `environment`: Current environment
     * Request/response examples for each endpoint
   - **Supported backend repositories:**
     * `projects/accounting-sso` (primary)
     * `projects/Accounting-Project` (fallback)
     * `projects/accounting-prototype` (fallback)
   - **If no endpoints found:** Skip Postman generation; note in taskclose.md

### PHASE 4: Taskclose Record Generation (FOURTH)

8. **Generate `<TASK-KEY>-taskclose.md`** — the single comprehensive task close record:

   **Output path:** `docs/work-items/02.implementation/stories/<STORY>/tasks/<TASK-KEY>-taskclose.md`

   **File must include frontmatter:**
   ```yaml
   ---
   title: "<TASK-KEY> - <task title> - Task Close"
   jira: <TASK-KEY>
   parent: <STORY-KEY>
   phase: Close
   created: <YYYY-MM-DD>
   closed: <YYYY-MM-DD>
   status: complete
   scope: <BACKEND|FRONTEND|FULLSTACK>
   ---
   ```

   **Required sections in the taskclose.md:**
   - **Section 1 — Execution Summary** table:
     - Start datetime / End datetime
     - Executed by
     - Jira status at task-close generation
     - Source branch / Target branch
     - GitLab issue (product repo): URL
     - GitLab MR (product repo): URL + state
     - GitLab issue (workspace): URL
     - GitLab MR (workspace): URL + state
     - Primary implementation commit(s)
   - **Section 2 — Delivered Changes** (subsections per domain layer):
     - Extract from `completion.md`: Description + Implementation Notes
     - Group by `01.Domain`, `02.Application`, `03.Infra`, `04.Presentation`, `tests`
     - Include file paths and per-file rationale
   - **Section 3 — Acceptance Criteria Status**:
     - List all AC/DoD items with ✓/✗ and per-item evidence
   - **Section 4 — API Endpoints** (if applicable):
     - Table of new/modified endpoints (Method, Path, Auth, Description)
     - Postman collection path
   - **Section 5 — Tests**:
     - Extract from `completion.md`: test commands and results
   - **Section 6 — Quality & Review Notes**:
     - Code quality, security checks, rollback strategy
     - Links: Jira task, implementation plan, completion.md, MRs

### PHASE 5: Completion Verification & Jira Comment (FIFTH)

9. Verify all operations completed:
   - Jira task now shows "In Review" status
   - Both MRs are marked Ready
   - `<TASK-KEY>-taskclose.md` exists and is complete
   - Postman collection updated (if applicable)
10. Add Jira comment linking all artifacts:
    ```
    Task closed and ready for review.

    ARTIFACTS
    - Task Close Record: docs/work-items/02.implementation/stories/<STORY>/tasks/<TASK>-taskclose.md
    - Completion Doc: docs/work-items/03.completation/linked/stories/<STORY>/Tasks/<TASK>/completion.md
    - Postman Collection: docs/work-items/02.implementation/stories/<STORY>/postman/<STORY>.postman_collection.json (if applicable)
    - MRs Ready for Review:
      * Workspace MR: [URL]
      * Project MR: [URL]

    NEXT STEPS
    1. Review MRs
    2. Approve changes
    3. Merge to develop
    4. Finalize task status
    ```
11. Create git commits for task close artifacts:
    - Commit message: `wip: <TASK-KEY> - Task closed and ready for review`
    - Include `<TASK-KEY>-taskclose.md` and updated Postman collection (if generated)

## Safety Rules

- **CRITICAL:** TaskClose does NOT call or invoke taskCompletion. It only verifies the completion.md file exists.
- If completion.md file is missing, stop immediately and exit without any Jira or GitLab changes.
- Never print raw credentials/tokens.
- Stop immediately if Jira task is not in "In Progress" status.
- Stop immediately if either MR is missing from Jira Web Links.
- If Jira transition fails, stop and report error without modifying MRs.
- Preserve MR review comments and existing review state.
- All artifacts (`<TASK-KEY>-taskclose.md`, updated Postman collection) must be committed to git.

## Conflict Prevention with TaskCompletion

- **TaskCompletion (03)** responsibility: Generate `completion.md` at `docs/work-items/03.completation/linked/stories/<STORY>/Tasks/<TASK>/completion.md`
- **TaskClose (04)** responsibility: Generate `<TASK>-taskclose.md` at `docs/work-items/02.implementation/stories/<STORY>/tasks/<TASK>-taskclose.md`, update Jira/GitLab states
- **No overlap:** taskClose does NOT recreate completion.md; it reads completion.md to build the taskclose record
- **Ordering enforced:** taskClose checks for completion.md existence as precondition

## Output Summary

Return:

- Jira task URL with new "In Review" status
- Workspace MR URL with "Ready" state
- Project MR URL with "Ready" state
- `<TASK-KEY>-taskclose.md` path
- Postman collection path (if generated/updated)
- Execution status summary with all 5 phases completed
