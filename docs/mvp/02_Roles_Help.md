**[راهنمای فرم مدیریت نقش‌ها]{dir="rtl"}**

[این فرم جایی است که **قوانین دسترسی** کل سیستم تعریف می‌شود]{dir="rtl"}.
[هر تغییری که اینجا می‌دهید، فوراً روی همه کاربرانی که آن نقش را دارند اثر
می‌گذارد]{dir="rtl"}.

**[این فرم دقیقاً چه کاری انجام می‌دهد؟]{dir="rtl"}**

-   [تعریف نقش‌های سازمانی (مثل «حسابدار»، «مدیر انبار»، «مدیر
    مالی»)]{dir="rtl"}

-   [اتصال کاربران به نقش‌ها]{dir="rtl"}

-   [تنظیم دقیق دسترسی‌ها در سه سطح]{dir="rtl"}:

**[سطح ۱ – دسترسی به فرم]{dir="rtl"}:** [کاربر اصلاً فرم را ببیند یا
نه]{dir="rtl"}.

[**سطح ۲ – عملیات داخل فرم:** کاربر داخل همان فرم چه کارهایی بتواند
انجام دهد؟ ایجاد سند، ویرایش، حذف، چاپ، تأیید، خروجی اکسل، اشتراک‌گذاری و
عملیات اختصاصی هر فرم]{dir="rtl"}

[**سطح ۳ – محدوده داده (مهم‌ترین سطح):** کاربر داخل همان فرم فقط کدام
داده‌ها را ببیند؟ **مثال واقعی**]{dir="rtl"}**:**

-   [کاربر فقط اسناد «عمومی» را ببیند، اسناد «افتتاحیه» را
    نبیند]{dir="rtl"}.

-   [کاربر فقط اسناد با وضعیت «پیش‌نویس» و «تأیید شده» را ببیند، اسناد
    «قطعی» را نبیند]{dir="rtl"}.

-   [کاربر فقط پروژه‌های خودش را ببیند]{dir="rtl"}.

-   [کاربر فقط شعبه خودش را ببیند]{dir="rtl"}.

**[نحوه کار با فرم]{dir="rtl"}**

[**ایجاد یا ویرایش نقش** عنوان نقش، کد سیستمی، وضعیت فعال و تاریخ اعتبار
را وارد کنید]{dir="rtl"}.

[**تخصیص کاربر به نقش** دکمه «کاربران نقش» را بزنید]{dir="rtl"} [کاربران
را جستجو و اضافه کنید]{dir="rtl"}.

**[تنظیم دسترسی‌ها (مهم‌ترین بخش)]{dir="rtl"}**

1.  [دکمه «دسترسی‌ها» را بزنید]{dir="rtl"}.

2.  [در درخت سمت چپ فرم مورد نظر را انتخاب کنید]{dir="rtl"}.

3.  [در سمت راست]{dir="rtl"}:

    -   [تیک عملیات مجاز را بزنید (سطح ۲)]{dir="rtl"}

    -   [تیک محدوده داده را بزنید (سطح ۳)]{dir="rtl"}

4.  [می‌توانید روی یک ماژول کامل «دسترسی کامل» یا «حذف همه» بزنید (روی
    همه زیرفرم‌ها هم اثر می‌کند)]{dir="rtl"}.

**[نکته خیلی مهم برای شما (کاربر)]{dir="rtl"}**

-   [تغییر دسترسی یک نقش]{dir="rtl"} [فوراً روی همه کاربران آن نقش اثر
    می‌گذارد (حتی کسانی که الان آنلاین هستند)]{dir="rtl"}.

-   [اگر دسترسی مستقیم به یک کاربر داده باشید (در فرم مدیریت کاربران)،
    دسترسی مستقیم همیشه اولویت دارد و دسترسی نقش را پوشش
    می‌دهد]{dir="rtl"}.

-   [بهترین روش: اول نقش بسازید، دسترسی‌ها را کامل تنظیم کنید، سپس
    کاربران را به نقش متصل کنید]{dir="rtl"}.
